version: 2

sources:
  - name: stripe
    description: A clone of a jaffle shop payment database.
    database: raw
    schema: stripe
    tables:
      - name: payment
        description: Raw Payment Data.
        # loaded_at_field: _batched_at
        # freshness:
          # warn_after: {count: 12, period: hour}
          # error_after: {count: 24, period: hour}